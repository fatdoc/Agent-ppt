# 图片生成 → 可编辑 PPT → 在线编辑

2026-09-26，基于集成提交 `f2215acb` 的后续实现。本文件说明代码与隔离验收，不表示生产已部署。

## 产品链路

大纲 → 逐页描述 → 图片生成（此处可直接导出图片版 PPTX/PDF/图片）→ 点击“生成可编辑 PPT” → 转换进度 → 生成文件与结构检查成功 → 自动进入 PPTist 在线编辑 → 保存版本/导出当前版本。

- Preview 新主入口支持全套页面；图片未齐时不可提交。
- 确认弹窗告知图片识别模型、预计积分及识别偏差。确认后才 POST，不会在进入预览时自动收费。
- 已有编辑文档显示“进入在线编辑”；不重复转换、不覆盖用户手动修改。
- 原来的导出能力保留；旧 OCR 导出菜单明确标注“旧版可编辑导出（仅下载）”，它不是在线编辑入口。
- 编辑文档与原图独立。后续重新生成原图，不会默默覆盖已编辑版本；目前没有“强制覆盖重建”入口。

## 实现边界

- 新 `GET/POST /api/projects/<project_id>/editable-generation`；GET 仅状态与报价，跳过 Provider/OAuth 加载；POST 经过认证、CSRF、归属检查和积分预留，捕获用户 Provider 快照。
- 使用现有 caption Provider 的图像接口逐页识别语义布局。Local 当前 Page 的大纲/描述作为文字辅助；它们不含准确布局坐标，所以图片仍须视觉识别。当前没有声称复用了不存在的结构化坐标。
- 模型只提供受限对象 JSON；用户/项目 ID、原图 hash、素材路径和 provenance 由服务端生成。局部图片由服务器裁切；模型不能指定文件路径或任意资源 URL。
- 原生段落、完整卡片形状、独立局部图像、原生表格及单层语义组合，复用已集成的 semantic Page、PPTistAdapter、native PPTX builder 和 OOXML audit。
- 不支持全页原稿贴底的伪可编辑结果；对越界、整页内容图片、危险/未知字段、不支持的对象、混合画布尺寸等拒绝发布。
- 成功页有独立 checkpoint；失败重试在相同进程/配置快照范围内复用。Provider 配置、原图或描述变化会使缓存键变化；缺失/损坏的缓存素材会重新生成。进程重启可能使 Provider cache scope 失效，不保证跨重启免调用模型。
- 全套转换与文件检查成功后，才原子创建 editor document/revision 和完成 Task/积分结算。发布前复核页面顺序、图片 hash、文字上下文，检测变化就中止。
- 已有编辑文档从不覆盖；部分失败不发布半成品，释放预留积分，原图保持不变。后台异常不向用户泄露 Provider 原始响应。
- 刷新预览可恢复后台任务轮询，任务成功自动跳转；断网读状态失败后提示重新检查，不盲目重复提交。工作进程重启的孤儿任务仍使用既有离线恢复命令，不伪装成已恢复运行。
- 没有新增模型或迁移；沿用 031 文档表和现有 Task/积分模型。任务进行中暂不允许删除项目；任务结束后删除时保留积分账本金额、余额快照、用户及记录，通过 legacy 字段保留被解除的项目/任务引用。

## 验证与证据

- 最终后端全量隔离回归：610 passed、8 skipped（外部服务/真实模型测试），119.69 秒，2093 warnings。日志 `/tmp/editor-flow-backend-final.log`。前端日志 `/tmp/editor-flow-frontend-final.log`。`git diff --check` 通过。

- 后端新增覆盖：完整图片→结构文档→可下载文件、重复提交/单次扣费、权限与缺图、失败退款/重试、源图漂移、已有文档保护、状态查询无 Provider、多页原子发布及成功页复用、转换中删除保护和完成后账本保全。
- 前端：100 项通过；新增覆盖确认后提交、缺图禁用、完成后自动导航、已有文档手动打开、失败留在预览、刷新恢复既有任务、网络错误后状态恢复、卸载后忽略迟到响应。TypeScript 和生产构建通过。
- Playwright CLI 独立浏览器，从未初始化编辑文档的图片项目开始：登录 → Preview → 查看确认/报价 → 发起转换 → 自动进入编辑器 → 原生文字改为包含“在线编辑通过” → 保存 → 实际下载 `image-flow-edited.pptx`。
- `unzip -t` 无错误；下载文件的 slide XML 包含新增中文、原生 shape/text、`p:grpSp` 和独立 `p:pic`。
- 返回 Preview 后是“进入在线编辑”，普通“导出”仍可用。再次用最终确认弹窗重跑自动跳转，恢复正常会话后浏览器为 0 console errors、2 warnings。
- 视觉检查发现顶栏滤镜导致 fixed 弹窗裁切，已用 body portal 修复并截图复核。证据：`output/playwright/image-flow-confirm-final.png`、`image-flow-edited.png`、`image-flow-edited.pptx`（不入 Git）。新增正文过长时仍需用户调整文本框尺寸，不宣称自动排版无溢出。
- 复现：`python scripts/verify_pptist_runtime.py --image-flow --port 5193 --output /tmp/pptist-image-flow-qa`。该脚本强制临时 DB/uploads、禁用 dotenv 与外网，使用明确的模拟识别响应；不会影响真实项目。

## 尚未验证 / 不应混淆

本次浏览器与后台端到端测试使用**模拟视觉识别响应**，其余转换、素材裁切、语义校验、原生 PPTX、版本保存及下载为真实执行。没有调用真实付费模型，不能据此断言复杂真实页面的文字召回、版式保真、图标或表格识别质量已经达标。

当前自动重建只支持语义 v1 能力：纯色背景和有限原生形状，复杂装饰采用局部图片，未自动重绘 SVG。原始图里的任意渐变、阴影、动画和特殊对象不保证无损复原。进入编辑器会明确提示核对。

上线下一步：部署当前代码/前端构建（旧环境须已具备 031），用用户明确选择的代表页和实际 caption Provider 做付费识别验收，再检查全套样例与 WPS/PowerPoint。此次没有部署、修改真实数据库、修改 `.env` 或解除单用户门禁。
